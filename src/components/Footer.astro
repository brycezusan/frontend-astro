---
const pathname = Astro.url.pathname
---
<footer class="text-lg bg-gradient-to-tr to-slate-800 from-slate-900/90 text-white ">
  <div class="container mx-auto py-12 flex flex-col gap-3 items-center md:flex-row-reverse md:justify-between">
    <p>Todos los derechos reservados ❤️ {new Date().getFullYear()}</p>

    <nav class="flex gap-1">
      <a href="/" class={`${pathname === '/' && 'bg-red-600'}`}>Inicio</a>
      <a href="/blog" class={`${pathname === '/blog'  && 'bg-red-600'}`}>Blog</a>
      <a href="/nosotros" class={`${pathname === '/nosotros'  && 'bg-red-600'}`}>Nosotros</a>
    </nav>
  </div>
</footer>

<style>
  a{
    padding-inline: 1rem;
    border-radius: 0.85rem;
    transition: all .2s ease-in;
  }
  a:hover{
    background-color: red;
  }
</style>